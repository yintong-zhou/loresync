// Form di inserimento: link + capitolo raggiunto, con estrazione titolo dal link.
// TODO: collegare a POST /api/metadata (anteprima titolo) e POST /api/manga (salvataggio).
export default function AddMangaPage() {
  return (
    <main className="flex flex-col gap-4">
      <h1 className="text-2xl font-semibold">Aggiungi una serie</h1>
      <p className="text-sm text-neutral-dark">
        Scaffolding: il form non e&apos; ancora implementato.
      </p>
    </main>
  );
}
